package com.smartcampus.api.model.enums;

public enum ResourceStatus {
    ACTIVE,         // Currently in use / occupied
    NOT_ACTIVE,     // Available for request
    OUT_OF_SERVICE  // Under maintenance / error
}
